{
  "name": "naturally-curious-publisher",
  "private": true,
  "type": "module",
  "scripts": {
    "crawl": "bun run crawl.ts",
    "upload": "bun run upload.ts",
    "all": "bun run crawl.ts && bun run upload.ts"
  },
  "dependencies": {
    "aws4fetch": "^1.0.20"
  }
}
